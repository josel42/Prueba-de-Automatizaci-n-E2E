webdriver {
  driver = chrome
  autodownload = true
}

chrome {
  switches = [
    "--disable-save-password-bubble",
    "--disable-password-manager",
    "--disable-notifications",
    "--disable-infobars",
    "--disable-extensions",
    "--start-maximized"
  ]
}
